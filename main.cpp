#include <iostream>
#include <vector>
#include <unordered_map>

// load image
// create ASCII array
// transform image to greyscale with floating point values from 0.0 to 1.0
// based on the floating point value, replace the pixel with an ASCII character that matches the luminosity density

int main() {
    std::vector<char> asciiChars = {
        ' ', '.', '`', ',', ':', ';', 'i', 'l', '!', '^', '+', '*', '?', 
        '%', 'S', '#', '@', 'M', 'W', '&', '8', 'B', '$'
    };

    return 0;
}
